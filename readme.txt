PREREQUISITES:

Django, openpyxl, matplotlib packages should be installed.

Open command window in this folder.

Run the following command : 'python manage.py runserver'

This will generate a link.
 
Now click the link and a browser opens.
 upload the sample file you gave.


